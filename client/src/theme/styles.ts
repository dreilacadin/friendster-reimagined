import { Styles } from "@chakra-ui/theme-tools"

const styles: Styles = {
  global: () => ({
    body: {
      fontFamily: "SF Pro Display",
      fontSize: "14px"
    }
  })
}

export default styles
